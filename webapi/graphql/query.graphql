type Query {
    # Fetches an object given its ID
    node(
        # The ID of an object
        id: ID!
    ): Node
    accounts:[Account!]
}
interface Node {
    # The id of the object.
    id: ID!
}